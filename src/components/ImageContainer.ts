// import fs from "fs";

export const setBackground = (element: HTMLDivElement): void => {
    const directoryPath = "assets/images";
    // try {
    //     const files = fs.readdirSync(directoryPath);
    //     console.log(files);

    // } catch (error) {
    //     console.error("Error reading directory:", error);
    // }
};
